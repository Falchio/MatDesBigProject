<resources>

    <style name="MainTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/secColor</item>
        <item name="colorPrimaryDark">@color/secDarkColor</item>
        <item name="colorAccent">@color/secAccentColor</item>
        <item name="android:textColorPrimary">@color/primaryTextColor</item>
        <item name="android:windowBackground">@color/secWindowBackground</item>
        <item name="android:navigationBarColor">@color/secNavBarColor</item>
    </style>


    <!-- Base application theme. -->
    <style name="SecondTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/accentColor</item>
        <item name="android:textColorPrimary">@color/primaryTextColor</item>
        <item name="android:windowBackground">@color/primaryWindowBackground</item>
        <item name="android:navigationBarColor">@color/primaryNavBarColor</item>
    </style>


    <style name="LoginAppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/secColor</item>
        <item name="colorPrimaryDark">@color/secDarkColor</item>
        <item name="colorAccent">@color/secAccentColor</item>
        <item name="android:textColorPrimary">@color/primaryTextColor</item>
        <item name="android:windowBackground">@color/secWindowBackground</item>
        <item name="android:navigationBarColor">@color/secNavBarColor</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="MainTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="SecAppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="SecAppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="for_button" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text">@string/identical_buttons</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
        <item name="android:layout_marginTop">70dp</item>
        <item name="layout_constraintHorizontal_bias">0.5</item>
    </style>

</resources>